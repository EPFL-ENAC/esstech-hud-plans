[project]
name = "scripts"
version = "0.1.0"
description = "List of dependencies for tests scripts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "open3d>=0.20.0",
    "pycolmap>=4.2.0",
    "typer>=0.27.2",
]
